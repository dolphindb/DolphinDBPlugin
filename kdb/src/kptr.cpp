#include <cassert>

#include "kptr.h"

//////////////////////////////////////////////////////////////////////////////

void KDeleter::operator()(K k) const {
    assert(!k || k->r >= 0);
    r0(k);
}

//////////////////////////////////////////////////////////////////////////////